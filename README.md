# FCM Payload Test

Fixed JVM target mismatch: both Java and Kotlin target JDK 17.

Build with GitHub Actions, then download artifact `fcm-payload-test-debug` and install `app-debug.apk` in Genymotion.
